# Full SDK Demo Scenario

Пример отдельного подпроекта сценария для оркестратора.

Цель:
- показать минимальную структуру внешнего git-репозитория сценария
- продемонстрировать весь актуальный публичный API `orchestrator-sdk`
- дать готовый `algorithm_path` для регистрации сценария в оркестраторе

## Структура

```text
full-sdk-demo-scenario/
├── README.md
├── requirements.lock
└── scenario.py
```

## Как использовать

1. Установить зависимости сценария:

```bash
pip install -r requirements.lock
```

Для локальной разработки внутри монорепозитория можно временно заменить строку в `requirements.lock` на:

```text
-e ../../sdk
```

После этого для локальной разработки можно устанавливать зависимости через:

```bash
pip install -r requirements.lock
```

Файл `requirements.lock` должен оставаться в репозитории сценария и использоваться worker для подготовки зависимостей.

2. Зарегистрировать сценарий в оркестраторе:

- `git_url`:
  URL git-репозитория, в котором лежит этот подпроект
- `version`:
  либо `latest`, либо имя git tag
- `algorithm_path`:
  `scenario.py`

Значение `latest` означает последнюю ревизию ветки, определяемой через `git_url`. Если нужен воспроизводимый запуск конкретной версии, в качестве `version` должно использоваться имя git tag.

3. Проверить ПО запуска сценария:

- на текущем этапе поле ПО запуска в интерфейсе недоступно для редактирования
- в нем отображается версия Python, под которой запущен worker, например `python3.11`
- выбор другой версии Python откладывается до перехода на внешнюю стратегию исполнения вроде `subprocess/venv`

4. Передать во входных данных сценария, например:

```json
{
  "task_description": "hello from scenario",
  "child_prompt": "child hello world",
  "cancel_prompt": "cancel me"
}
```

## Требования к сценарию

- Сценарий должен содержать `requirements.lock`.
- Если сценарий должен запускаться в фиксированной версии, в репозитории должен существовать git tag, имя которого используется как значение `version`.
- Сценарий должен быть совместим с той версией Python, под которой запущен worker.
- Если сценарию требуется конкретная версия Python, это должно быть явно указано в README репозитория сценария, но на текущем этапе worker не сможет переключиться на другой интерпретатор.
- Сценарий должен фиксировать версию `orchestrator-sdk` в `requirements.lock`.
- Во время прогрева worker выполняет compatibility-check версии `orchestrator-sdk`; результат такой проверки имеет уровень warning и не должен сам по себе блокировать запуск.

## Что показывает пример

- `sdk.log(...)`
- `sdk.generate(...)`
- `sdk.create_request(...)`
- `sdk.get_request_status(...)`
- `sdk.wait_for_completion(...)`
- `sdk.cancel_request(...)`
- `sdk.emit_metric(...)`
