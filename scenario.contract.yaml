schema_version: 1

model_types:
  - type_key: planner
    display_name: Planner
    description: Builds the primary answer for the demo scenario.
    is_required: true
    sort_order: 10
    constraints:
      modality: text
      task_mode: generation

  - type_key: critic
    display_name: Critic
    description: Reviews the primary answer and returns a short verdict.
    is_required: true
    sort_order: 20
    constraints:
      modality: text
      task_mode: generation
